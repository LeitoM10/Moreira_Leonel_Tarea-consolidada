# Proyecto Java - Más de León

Proyecto Java proporcionado por el usuario. Contiene clases `Main.java` y `Vendedor.java`.

Instrucciones:
1. Crea un repositorio remoto (GitHub/GitLab/Bitbucket) vacío.
2. Pega el link aquí y yo añadiré el `remote` y haré el `push` por ti.

Estructura:
- `src/` — código fuente Java

He inicializado el repositorio local y preparado un commit inicial. Cuando me mandes el link remoto lo conectaré y haré push.
